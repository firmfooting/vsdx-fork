# Roadmap: vsdxkit modernisation

Distribution `vsdxkit` (import `vsdx`), fork of dave-howard/vsdx.
Base: upstream v0.6.1 + Visio-faithful feature set (see git log).

## Done

- [x] Annotation modernisation to PEP 604 unions (Pyrefly 0 errors)
- [x] `pyproject.toml` packaging; setup.py/requirements.txt removed;
      version single-sourced from `vsdx.__init__.__version__`
- [x] Distribution renamed `vsdxkit`; import stays `vsdx`; wheel+sdist
      build verified by install-import test
- [x] ruff adopted (default rules + import sorting + formatting);
      remaining findings are the documented ratchet
- [x] pyrefly adopted (preset `basic`, ratchet start)
- [x] `vsdxfile.py` slimmed 1,292 → ~982 lines: masters-import →
      `masters.py`, jinja templating → `templating.py`, zip-XML
      persistence → `xmlio.py`; `VisioFile` keeps the full public API
      via mixins; `python_requires >= 3.10` (EOL 3.7–3.9 dropped)
- [x] v0.6.3 release (tag + wheel)

## Next

### R1 — CI rebuild (next)

- [ ] replace `.github/workflows/test.yaml`: actions/checkout@v4 +
      setup-python@v5, SHA-pinned; pytest matrix 3.10–3.14 ×
      (ubuntu, windows); ruff check + `ruff format --check`; pyrefly
      check; build wheel on release tags
- [ ] enable Actions on the fork (they were dark: last run Jan 2026)
- [ ] `py.typed` ships in the wheel (marker already added)

### R2 — ruff ratchet

- [ ] fix or explicitly ignore the remaining 61 findings (RUF013
      implicit-optional is the big one); then add ruff to pre-commit
      hygiene with `--exit-zero` removed forever
- [ ] enable E402 suppression only where the import-cycle structure
      requires late imports; document each site

### R3 — type coverage ratchet

- [ ] pyrefly preset step: basic → standard after RUF013 cleanup
- [ ] annotate `shapes.py` and `pages.py` public APIs; aim 60%+ hint
      coverage; `py.typed` then carries real information

### R4 — API rationalisation (v1.0)

- [ ] `save_vsdx(None)` saves in place (currently raises AttributeError)
- [ ] deprecate `Connect.create()` in favour of `Page.connect_shapes`
- [ ] unify `remove_page_by_index`/`remove_page_by_name` with
      `delete_shape` semantics (records + connectors)
- [ ] docs: README rewrite around fork capabilities (connect_shapes,
      create_shape, swimlanes, reanchor_connector), API docs refresh

### R5 — release engineering

- [ ] GitHub Release workflow: wheel + sdist artifacts on `v*` tags
- [ ] changelog from conventional commits

## Non-goals

- Upstream merge-back of the feature set (upstream PRs #95/#96 carry
  the universal-bug subset; the rest is fork-forward)
- Renaming the import namespace (`vsdx` stays; only the distribution
  name changed)
